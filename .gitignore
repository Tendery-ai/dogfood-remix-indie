# Upstream (a template you scaffold from) ignores every lockfile so adopters can
# pick their own package manager. This fork is not a template — it is a dogfood
# fixture whose install time and turn counts get measured, and that needs one
# reproducible install. So npm's lockfile is committed; the rest stay ignored to
# keep the choice unambiguous.
# This part will be removed by `remix.init`
bun.lockb
pnpm-lock.yaml
pnpm-lock.yml
yarn.lock

node_modules

/build
/public/build
# Upstream ignores .env so scaffolded apps do not commit secrets. This fork
# is a dogfood fixture: Prisma and Remix read DATABASE_URL / SESSION_SECRET
# from .env, and CI already copies .env.example. The committed file is that
# example (sqlite + placeholder secret), not a live credential.

/cypress/screenshots
/cypress/videos
/prisma/data.db
/prisma/data.db-journal
